{
    "scripts": {
        "clean": "rimraf ./dist",
        "build": "tsc --project ./tsconfig.json",
        "start": "node dist/index.js",
        "rerun": "npm run clean && npm run build && npm run start"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "^4.5.5"
    },
    "dependencies": {
        "express": "^4.17.2",
        "pug": "^3.0.2"
    }
}
