{
    "scripts": {
        "clean": "rimraf ./dist",
        "build": "tsc --project ./tsconfig.json --module es2015 --outDir ./dist -d",
        "start": "node dist/index.js"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "^4.5.5"
    }
}
